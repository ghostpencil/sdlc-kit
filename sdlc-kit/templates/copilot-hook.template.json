{
  "version": 1,
  "hooks": {
    "postToolUse": [
      {
        "type": "command",
        "matcher": "edit|create|apply_patch",
        "timeoutSec": 120,
        "bash": "i=$(cat); emit(){ python -c \"import sys,json;print(json.dumps({'additionalContext':sys.stdin.buffer.read().decode('utf-8','replace')[:8000]}))\"; }; o=$(printf '%s' \"$i\" | python -c \"import sys,json;d=json.loads(sys.stdin.buffer.read().decode('utf-8','replace'));a=d.get('toolArgs') or d.get('tool_input') or {};a=json.loads(a) if isinstance(a,str) else a;print(a.get('path') or a.get('file_path') or a.get('filePath') or a.get('filename') or a.get('file') or '');print(d.get('cwd') or '')\" 2>/dev/null); f=$(printf '%s' \"$o\" | head -n 1); w=$(printf '%s' \"$o\" | tail -n 1); if [ -n \"$w\" ]; then cd \"$w\" || exit 0; fi; if [ -z \"$f\" ]; then printf '%s' 'SDLC gate hook did NOT run on this edit: no file path found in the hook payload. The gate did not check this file - fix the hook before trusting it (discovery procedure: COPILOT.md in the kit folder or the kit home repository).' | emit; exit 0; fi; case \"$f\" in {{SOURCE_GLOB}}) ;; *) exit 0 ;; esac; if [ ! -f \"$f\" ]; then printf 'SDLC gate hook did NOT run: the edited file was not found from the hook working directory: %s' \"$f\" | emit; exit 0; fi; l=$({{HOOK_LINT_CMD}} 2>&1); lrc=$?; t=\"\"; trc=0; {{HOOK_TYPECHECK_BLOCK}} if [ $lrc -ne 0 ] || [ $trc -ne 0 ]; then { echo 'SDLC gate hook: lint/typecheck failed on the file just edited. Fix it before continuing:'; echo \"$l\"; echo \"$t\"; } | emit; fi; exit 0"
      }
    ]
  }
}
